{
    "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentTemplate.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
        "environment": {
            "type": "string",
            "metadata": {
                "description": "A moniker for the environment."
            }
        },
        "location": {
            "type": "string",
            "defaultValue": "[resourceGroup().location]",
            "metadata": {
                "description": "The location where the resources will be deployed."
            }
        },
        "administratorLogin": {
            "type": "string",
            "defaultValue": "[concat(parameters('environment'), '-sql-admin')]"
        },
        "administratorLoginPassword": {
            "type": "securestring"           
        },
        "dataSubnetId":{
            "type": "string"
        }
    },
    "variables": {
        "sqlServerName": "[concat(parameters('environment'), '-azure-sql-instance')]"
    },
    "resources": [
        {
            "type": "Microsoft.Sql/servers",
            "apiVersion": "2019-06-01-preview",
            "name": "[variables('sqlServerName')]",
            "location": "[parameters('location')]",
            "properties": {
                "administratorLogin": "[parameters('administratorLogin')]",
                "administratorLoginPassword": "[parameters('administratorLoginPassword')]",
                "location": "[parameters('location')]",
                "version": "12.0",
                "minimalTlsVersion": "1.2"
            }
        },
        {
            "type": "Microsoft.Sql/servers/virtualNetworkRules",
            "apiVersion": "2015-05-01-preview",
            "name": "[concat(variables('sqlServerName'), '/allow-app-to-iiq-dbs')]",
            "dependsOn": [
                "[variables('sqlServerName')]"
            ],
            "properties": {
                "virtualNetworkSubnetId": "[concat(parameters('virtualNetworks_sandbox_vnet_externalid'), '/subnets/sandbox-dataSubnet')]",
                "ignoreMissingVnetServiceEndpoint": false
            }
        }
    ],
    "outputs": {
    }
}