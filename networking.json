{
    "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentTemplate.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
        "resourceGroup": {
            "type": "string",
            "defaultValue": "[resourceGroup().name]"
        },
        "location": {
            "type": "string",
            "defaultValue": "[resourceGroup().location]",
            "metadata": {
                "description": "Specifies the location for all resources."
            }
        },
        "VNetSettings": {
            "type": "object"
        }
    },
    "variables": {
        "vNetName": "[concat(parameters('resourceGroup'), '-', parameters('VNetSettings').VNetName)]"
    },
    "resources": [
        {
            "name": "[variables('vNetName')]",
            "type": "Microsoft.Network/virtualNetworks",
            "location": "[resourceGroup().location]",
            "apiVersion": "2020-05-01",
            "tags": {
                "displayName": "[variables('vNetName')]"
            },
            "properties": {
                "addressSpace": {
                    "addressPrefixes": [
                        "[parameters('VNetSettings').VNetAddressPrefix]"
                    ]
                },
                "copy": [
                    {
                        "name": "subnetsCopy",
                        "count": "[length(parameters('VNetSettings').subnets)]",
                        "input": {
                            "name": "[parameters('VNetSettings')[copyIndex('subnetsCopy')].name]",
                            "properties": {
                                "addressPrefix": "[parameters('VNetSettings')[copyIndex('subnetsCopy')].addressPrefix]"
                            }
                        }
                    }
                ]
            }
        }
    ]
}